# CMakeLists.txt per il main
cmake_minimum_required(VERSION 3.16)

# File sorgenti del main
set(MAIN_SOURCES src/main.cpp)

# Nota: l'eseguibile viene creato nel CMakeLists.txt del simulatore
# Questo file serve principalmente per organizzazione
